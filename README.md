# 📊 Income/Expense Tracker LINE Bot

โปรเจกต์ LINE Bot สำหรับบันทึกรายรับ-รายจ่ายประจำวัน ถูกพัฒนาด้วย Python (Flask) โดยสามารถรับข้อความจากผู้ใช้งานผ่าน LINE Messaging API และรันเซิร์ฟเวอร์แบบ Local ผ่าน ngrok 

## 🌟 ฟีเจอร์ (Features)
- 🟢 **บันทึกรายรับ**: พิมพ์ `รับ <รายการ> <จำนวนเงิน>` (เช่น `รับ เงินเดือน 50000`)
- 🔴 **บันทึกรายจ่าย**: พิมพ์ `จ่าย <รายการ> <จำนวนเงิน>` (เช่น `จ่าย ค่าอาหาร 150`)
- 📊 **สรุปยอดเงิน**: พิมพ์ `สรุป` เพื่อดูสรุปรายรับรวม รายจ่ายรวม กราฟสัดส่วนรายจ่าย และเงินคงเหลือ
- 📜 **ประวัติ**: พิมพ์ `ประวัติ` เพื่อดูรายการล่าสุุด 5 รายการ
- 🗑️ **ล้างข้อมูล**: พิมพ์ `ล้างข้อมูล` เพื่อเคลียร์ข้อมูลปัจจุบัน (ข้อมูลเก็บลงฐานข้อมูล SQLite แยกตาม `user_id` ของแต่ละคน)

## 📋 ข้อกำหนดเบื้องต้น (Prerequisites)
ก่อนเริ่มรันโปรเจกต์ คุณจำเป็นต้องมีสิ่งเหล่านี้:
- Python 3.9+ ขึ้นไป
- บัญชี [LINE Developers](https://developers.line.biz/en/) เพื่อสร้าง Messaging API Channel (เอาไว้ดึง Token)
- บัญชี [ngrok](https://ngrok.com/) สำหรับสร้าง Public URL (เพื่อทำ Webhook URL)

## 🚀 การติดตั้งและใช้งาน (Installation & Usage)

1. **เตรียมไฟล์โปรเจกต์**
   ทำมั่นใจว่าคุณมีไฟล์ `main.py`, `requirements.txt`, `.env` และอยู่ในโฟลเดอร์เดียวกัน

2. **ติดตั้งไลบรารีที่จำเป็น**
   รันคำสั่งนี้ใน Terminal:
   ```bash
   pip install -r requirements.txt
   ```

3. **ตั้งค่าตัวแปรสภาพแวดล้อม (Environment Variables)**
   สร้างไฟล์ `.env` ในโฟลเดอร์เดียวกัน (ถ้ายังไม่มี) และกรอกข้อมูล Token ที่ได้มา โดยรูปแบบด้านในเป็นดังนี้:
   ```env
   CHANNEL_ACCESS_TOKEN="YOUR_LINE_CHANNEL_ACCESS_TOKEN"
   CHANNEL_SECRET="YOUR_LINE_CHANNEL_SECRET"
   NGROK_AUTH_TOKEN="YOUR_NGROK_AUTH_TOKEN"
   ```

4. **รันระบบ (Run the Bot)**
   รันไฟล์ `main.py`:
   ```bash
   python main.py
   ```
   เมื่อโค้ดทำงานสำเร็จ ใน Terminal จะขึ้นข้อความที่มี URL ของ ngrok เช่น:
   `Copy this URL and insert it into LINE Webhook.: https://xxxx.ngrok-free.app/callback`

5. **ตั้งค่า Webhook ใน LINE Developers Console**
   - ไปที่ตั้งค่าของ LINE Messaging API
   - นำ URL ที่ได้จากขั้นตอนที่แล้ว ไปกรอกในช่อง **Webhook URL**
   - เปิดสวิตช์ใช้งาน (Enable) **Use webhook**
   - แนะนำให้ **ปิดระบบ Auto-reply messages** ในหน้าตั้งค่า LINE Official Account เพื่อไม่ให้บอทพื้นฐานตอบซ้อนกับบอทของเรา

## 📂 โครงสร้างโค้ด (File Structure)
- `main.py` : ไฟล์โปรเจกต์หลักที่รันเซิร์ฟเวอร์ Flask, และรับข้อมูลจาก LINE API
- `requirements.txt` : ไฟล์เก็บรายชื่อไลบรารีที่จำเป็น
- `.env` : ไฟล์เก็บข้อมูลความลับของ Token ป้องกันการถูกเผยแพร่ออกนอกระบบ
- `README.md` : ไฟล์อธิบายการใช้งาน (ไฟล์นี้)
- `money_tracker.db` : (สร้างอัตโนมัติ) ฐานข้อมูล SQLite เก็บรายรับ-รายจ่าย
- `static/` : (สร้างอัตโนมัติ) โฟลเดอร์เก็บไฟล์ภาพกราฟที่ Generate ออกมา
